<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml
  Role: declares app permissions and the launcher activity.
  - INTERNET permission is required for Retrofit to make network calls.
  - usesCleartextTraffic="true" allows plain http:// (only for this local demo!
    Real apps must use https://).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="LoginApp"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
